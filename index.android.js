import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

const MathsApp = props => (
  <App />
)

AppRegistry.registerComponent('MathsApp', () => MathsApp);
